{
  "name": "esmx-ai-demo-root",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "start": "pnpm --filter host-app --filter react-app --parallel run start",
    "clean": "rm -rf apps/*/dist"
  }
}
